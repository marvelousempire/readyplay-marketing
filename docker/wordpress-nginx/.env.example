# Copy to .env and replace values before `docker compose up`.
# Never commit .env.

MYSQL_ROOT_PASSWORD=change-me-root
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=change-me-db

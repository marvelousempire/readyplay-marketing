# Used when this repo is connected to Netlify with base directory set to `web`.
# See docs/Marketing-site-hosting.md

[build]
  command = "npm ci && npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "22"
